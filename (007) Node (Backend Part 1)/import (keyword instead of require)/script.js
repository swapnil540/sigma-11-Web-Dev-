import {sum, PI} from "./math.js"; // importing from file
import { generate, count } from "random-words"; // imprt statement instead of require, importing packages

console.log(sum(2, 3));

console.log(generate());