// Qs.1 random number representing dice roll (range 1 to 6 )
let diceRoll = Math.floor(Math.random() * 6) + 1;
console.log(diceRoll);


// Qs.2 object car
const car = {
    name: "Defender",
    model: "Land Rover defender 110",
    color: "White"
};

console.log(car.name);

// Qs.3 object person
const person = {
    name: "Trun",
    age: 25,
    city: "bhandara",
}

person.city = "New york";
person.country = "United states";
console.log(person);
